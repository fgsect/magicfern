//
// Created by henrik on 19.06.17.
//

#ifndef MAGICFERN_TEST_H
#define MAGICFERN_TEST_H

using namespace cv;

int testMe(){
    /*
    std::cout << " HI ";
    Mat m = Mat(4, 4, CV_64FC3, Scalar(1.0d, 1.0d, 1.0d));
    Mat t = Mat(3, 3, CV_64FC3, Scalar(1.5d, 1.5d, 1.5d));
    //const int arrayHilfs[] = {500, 500, 3};
    //const int * array = arrayHilfs;
    //cv::Mat testMat = cv::Mat::ones(3, array, CV_64FC3);//500, 500, CV_64FC3);

    for(int i = 0; i < t.rows; i++){
        for(int j = 0; j < t.cols*3; j++){
            m.at<double>(i, j) = t.at<double>(i, j);
        }
    }


    std::cout << m << std::endl;
    //std::cout << "end" << std::endl;
    */

    /*
    cv::Point tester (42, 12);      //tester.x = 42

    std::cout << tester.x << std::endl;
    */

    return 0;
}

#endif //MAGICFERN_TEST_H
